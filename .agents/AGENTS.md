# Workspace Rules

- The user does not have Android Studio installed locally.
- All Android applications (APKs) must be built using GitHub Actions. Do not ask the user to open Android Studio or run Gradle builds locally.
